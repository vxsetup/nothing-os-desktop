input {
    kb_layout = us,ru   # Replace 'us,ru' with your desired layouts (e.g., us,es or us,fr)
    kb_variant =       # Leave blank for standard variants
    
    # Shortcut to toggle between layouts
    # Options include grp:alt_shift_toggle, grp:win_space_toggle, or grp:alt_space_toggle
    kb_options = grp:win_space_toggle
}
